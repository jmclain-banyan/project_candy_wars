import { Dispatch } from "redux";

export const STAGE_TRANSACTION: string = 'STAGE_TRANSACTION';
export const SUBMIT_SALE: string = 'SUBMIT_SALE';
export const SUBMIT_PURCHASE: string = 'SUBMIT_PURCHASE';

export const stageTransaction = (arg: any) => async (dispatch: Dispatch): Promise<void> => {
    
}

export const submitSale = (arg: any) => async (dispatch: Dispatch): Promise<void> => {
    
}

export const submitPurchase = (arg: any) => async (dispatch: Dispatch): Promise<void> => {
    
}
